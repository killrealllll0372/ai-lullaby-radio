{
  "name": "ai-lullaby-radio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "smoke-test": "node scripts/smoke-test.mjs",
    "test": "npm run smoke-test"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest"
  }
}
